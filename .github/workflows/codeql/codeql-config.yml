name: "Custom CodeQL Config"

queries:
- uses: security-extended
- uses: javascript-security-and-quality
paths:
  - src
paths-ignore:
  - node_modules
  - '**/*.test.ts'
  - '**/*.spec.ts'
  - 'dist/**'
  - 'tests/**'
  - 'coverage/**'