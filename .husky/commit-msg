#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Skip verification for commits generated by semantic-release (containing "[skip ci]")
if grep -q "\[skip ci\]" "$1"; then
  exit 0
fi

# Otherwise, run commitlint as usual
npx --no -- commitlint --edit "$1"